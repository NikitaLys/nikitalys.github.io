document.addEventListener('DOMContentLoaded', function() {
    $(function() {
        $("#accordion").accordion({
                collapsible: true,
                heightStyle: "content",
                navigation: true
            })
            // $(".catalog__period-btn-container").accordion("tabindex", "0", 0);
    })
})
